# Photon llms.txt Cache

This file is intentionally a placeholder.

Set `PHOTON_LLMS_URL` and run:

```bash
node --import tsx skills/photon-spectrum/scripts/fetch-photon-docs.ts
```

The script will replace this file with the current Photon llms.txt content.
