import metadata from "../project-metadata.json";

export const PROJECT_NAME = metadata.projectName;
export const PACKAGE_NAME = metadata.packageName;
export const PROJECT_VERSION = metadata.version;
export const REPOSITORY_SLUG = metadata.repositorySlug;
export const CANONICAL_REPOSITORY = metadata.canonicalRepository;
export const UPGRADE_COMMAND = metadata.upgradeCommand;
export const DEFAULT_CONFIG_DIRECTORY = metadata.defaultConfigDirectory;
export const DEFAULT_USER_ID = metadata.defaultUserId;
export const DEFAULT_BRANCH = metadata.defaultBranch;
